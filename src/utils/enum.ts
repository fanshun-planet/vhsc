export const enum MapModeEnum {
    Online = 'online',
    Offline = 'offline',
}

export const enum BMapSDKLoadStateEnum {
    Success = 'success',
    Fail = 'fail',
}

export const enum PipeTypes {
    supply = 'supply',
    return = 'return',
    device = 'device',
}